import React from "react";
import { Typography, Box } from "@mui/material";

const Watchlist: React.FC = () => {
    return (
        <Box sx={{ p: 4 }}>
            <Typography variant="h4">Watchlist Page</Typography>
        </Box>
    );
};

export default Watchlist;
