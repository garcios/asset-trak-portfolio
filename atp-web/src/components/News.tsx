import { Typography, Box } from "@mui/material";

const News = () => {
    return (
        <Box sx={{ p: 4 }}>
            <Typography variant="h4">News Page</Typography>
        </Box>
    );
};

export default News;
